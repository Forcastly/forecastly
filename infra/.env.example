# Optional overrides for infra/docker-compose.yml. Copy to infra/.env.
# Defaults (used when unset) match apps/backend/.env.example DATABASE_URL.
POSTGRES_USER=forecastly
POSTGRES_PASSWORD=forecastly
POSTGRES_DB=forecastly
POSTGRES_PORT=5432
